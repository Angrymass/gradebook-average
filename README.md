#gradebook-average
Utility for calculating grade averages from an electronic gradebook.
I can't publish my gradebook's api, so the api for the gradebook has to be put in a file named config.json, category "api", in the .exe/.py directory.
Example:
{
"api": "https://api.example.com"
}